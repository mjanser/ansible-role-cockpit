---
- hosts: all
  pre_tasks:
    - name: ensuare nginx is installed
      package:
        name: nginx
    - name: ensure nginx service is running
      service:
        name: nginx
        enabled: yes
        state: running
  roles:
    - ansible-role-cockpit
